# M14.6 — AI Core upstream health + registration checklist

Date of probe: 2026-04-17 (UTC).

## Live endpoint probes

Target: `https://ai.thronoschain.org/tx/submit`

Observed outcomes from this repo environment:

1. Plain GET probe returned `HTTP 503` with body containing `Service Suspended`.
2. Plain POST probe (`{}`) returned `HTTP 403` with body `Method forbidden`.

Interpretation:

- route DNS/edge is reachable, but the backend route is not currently in a healthy submit-ready state
- this is upstream gateway/service availability or routing policy, not local passmanager request-shape breakage

## Upstream verification checklist

### 1) AI Core service health + route mapping

- confirm `ai.thronoschain.org` is mapped to active AI Core service for `/tx/submit`
- confirm upstream runtime is not in suspended mode
- confirm POST is allowed for `/tx/submit` on the active service

### 2) AI registry/service identity path

- confirm `AI_SERVICE_REGISTER` path is available in the authoritative backend repo/environment
- confirm service registration normalizes pubkey to lowercase before comparison
- confirm the same lowercase pubkey used by pawssworfmanager is in the registry

### 3) scope assignment

- confirm registry entry includes `ai:attest` scope for the attestor pubkey
- if scope is missing, apply scope update and re-test submit

## Expected runtime outcomes after upstream recovery

Once endpoint health is restored, expected gate outcomes are:

- `UNREGISTERED_SERVICE` if attestor identity was never registered
- `MISSING_SCOPE` if `ai:attest` is not assigned
- submit acceptance with a transaction receipt if registry/scope is valid

## Local policy reminder

- do not add speculative auth-header patches for AI Core `/tx/submit`
- only patch local code if upstream evidence shows concrete body-contract mismatch
