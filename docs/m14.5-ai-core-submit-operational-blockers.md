# M14.5 — AI Core submit operational blocker classification

## Confirmed local contract state

- submit endpoint target: `https://ai.thronoschain.org/tx/submit`
- submit body uses:
  - `tx_type = AI_ATTESTATION`
  - `attestor_pubkey`
  - `attestor_signature`
  - required payload structure
- no dependence on Bearer/X-Internal-Key as the primary AI Core gate for this route

## Current blocker classification

Given current live result (`HTTP 503 Service Unavailable` surfaced as `attestation_rpc_unreachable`), the likely blocker is upstream AI Core availability and/or attestor registry prerequisites, not local deterministic/orchestrator plumbing.

## Expected next upstream-gate outcomes once service is available

- unregistered attestor service identity (e.g. `UNREGISTERED_SERVICE`)
- missing scope for attestor pubkey (e.g. `MISSING_SCOPE` for `ai:attest`)

## Local next action policy

- do not add blind header-auth tweaks for this specific AI Core route
- only patch locally again if upstream contract evidence shows a concrete request-shape mismatch
